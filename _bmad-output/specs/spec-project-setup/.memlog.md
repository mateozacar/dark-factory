---
topic: Dark Factory project scaffold — initialize the 4-layer clean architecture repository with all technologies defined in the Architecture Spine
updated: 2026-08-13T09:09
---

- (direction) User intent: create the first story for project setup with all technologies from the Architecture Spine (AD-1 through AD-11)
- (decision) Slug chosen: project-setup — describes the thing being specced (repository initialization and scaffold), not the input shape
- (decision) Source adopted: ARCHITECTURE-SPINE.md (full contract, must be read by downstream) — not absorbed, downstream still needs it
- (decision) Source read: brief.md (Product Brief) — confirms no persistence, no auth, no frontend in v1; aligns with non-goals
- (capability) CAP-1 — Developer installs all project dependencies via uv sync; success: uv sync exits 0, all packages importable without errors
- (capability) CAP-2 — Four-layer directory scaffold (domain/, application/, infrastructure/, interface/) exists under src/dark_factory/; success: all modules are importable and no cross-layer import violations (domain has zero external imports)
- (capability) CAP-3 — FastAPI app factory (create_app) returns a runnable ASGI app; success: uvicorn dark_factory.main:app starts with no import errors and /health, /docs, /redoc endpoints return 200
- (capability) CAP-4 — pydantic-settings Settings class loads configuration from environment variables; success: Settings() instantiates successfully with .env or env vars, all documented variables validated
- (capability) CAP-5 — GitHub Actions ci.yml runs ruff, mypy, and pytest against the scaffold; success: ci.yml passes on main branch after setup (all checks green)
- (constraint) Python 3.13 only (AD-1) — prevents use of any Python 3.12 or earlier runtime features; bends CI and Dockerfile targets
- (constraint) uv is sole package manager (AD-11) — no pip, no poetry, no requirements.txt; pyproject.toml is single source of truth; bends all install scripts and CI commands
- (constraint) domain/ must have zero external dependencies (AD-2) — no import fastapi, no import httpx, no import pydantic inside domain/; enforced by mypy and import analysis
- (constraint) All config via pydantic-settings + env vars only (AD-10) — no hardcoded URLs, secrets, or magic strings; bends how Settings is structured and what .env.example documents
- (constraint) TDD discipline: test file written before implementation file for every module (AD-4) — bends story order; empty test stubs must exist before implementation stubs
- (decision) Non-goal: USGS integration (USGSAdapter, httpx calls) — not in scope for setup story; deferred to infrastructure story
- (decision) Non-goal: HTTP endpoint implementation (GET /earthquakes, GET /health logic) — scaffold wires the router mount point; actual handlers deferred
- (decision) Non-goal: deploy.yml CI workflow — Render deploy hook setup deferred to deployment story after endpoints are implemented
- (assumption) Assumed 'project setup' means scaffold only: directory structure, pyproject.toml, app factory, config, empty domain/app/infra stubs, and CI; full feature logic is subsequent stories
- (assumption) Assumed tests/ structure mirrors src/dark_factory/ per AD-4; conftest.py and empty test stubs are part of setup
- (event) SPEC.md derived and written: 5 capabilities (CAP-1 to CAP-5), 5 constraints, 5 non-goals, 10 acceptance criteria
- (event) Pass 1 Coherence: all 5 caps have intent+success (rule 1 OK); intents describe WHAT not HOW (rule 2 OK); all 5 constraints rule something out (rule 3 OK); 5 explicit non-goals (rule 4 OK); success signals are demonstrable (rule 5 OK); CAP-1 through CAP-5 stable and unique (rule 6 OK); prose is lean (rule 8 OK)
- (event) Pass 2 Preservation: all load-bearing claims from Architecture Spine (AD-1 to AD-11) land in SPEC.md capabilities or constraints; endpoints table and deferred items map to non-goals; ARCHITECTURE-SPINE.md adopted as companion so downstream reads it; wrapper ceremony dropped
- (decision) Story 1 proposed: 'Dark Factory — Project Scaffold Setup'; spec_checkpoint=true (user), done_checkpoint=true (user), invoke_dev_with='' (user said none needed)
- (event) stories.yaml written and validated against schema: 1 story, id='1' (string-quoted), no status field, ids unique, prefix-free, all required fields present
- (event) GitHub issue #1 created at https://github.com/mateozacar/dark-factory/issues/1 with labels user-story and spec-ready
- (event) Spec finalized. on_complete workflow completed: consistency verified, issue created, no project board found.
