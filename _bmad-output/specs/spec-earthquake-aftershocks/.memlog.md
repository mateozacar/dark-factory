---
topic: Aftershock sequence detection for a given USGS earthquake event ID
updated: 2026-08-18T09:47
---

- (decision) Mode: headless express — input is complete and structured; all gaps become open_questions
- (decision) Slug: earthquake-aftershocks; spec folder: _bmad-output/specs/spec-earthquake-aftershocks/
- (decision) Feature: given a USGS earthquake event ID, detect its aftershock sequence and return structured result with stats and sequence_assessment
- (decision) Wells-Coppersmith radius formula: radius_km = 10 ** (0.5 * magnitude - 1.8); derived from seismological standard
- (decision) Aftershock search window: configurable 'days' param, default 30, max 90, ge=1; events AFTER main shock time
- (decision) Aftershock magnitude bounds: [main_mag - 3.0, main_mag - 0.1]
- (capability) CAP-1: Earthquake entity gains a 'time' field (ISO-8601 string from USGS properties.time ms-since-epoch); needed by CAP-3 for half-window split
- (capability) CAP-2: AftershockResult value object in domain — main_event (id,mag,depth,lat,lon,time), aftershocks list, stats (count,max_mag,avg_mag,largest_id), sequence_assessment enum
- (capability) CAP-3: GetAftershocksHandler in application layer — calls get_by_id, computes radius+window, calls get_all with extended EarthquakeFilter, computes sequence_assessment, returns AftershockResult
- (capability) CAP-4: USGSClient.fetch_earthquake_by_id implemented — GET /query?format=geojson&eventid={id}; USGSAdapter.get_by_id calls it and maps via USGSMapper; returns None on USGS 404
- (capability) CAP-5: EarthquakeFilter gains 4 new optional fields: latitude, longitude, max_radius_km, max_magnitude; USGSClient.query gains matching params
- (capability) CAP-6: USGSMapper.feature_to_earthquake extended to map 'time' from properties.time (ms-epoch int → ISO-8601 string)
- (capability) CAP-7: GET /api/v1/earthquakes/{earthquake_id}/aftershocks route; query param days (int, default 30, ge=1, le=90); returns AftershockResponse; 404 if main event not found; 502 if USGS error on main event fetch
- (capability) CAP-8: Unit tests for GetAftershocksHandler — decaying sequence, active sequence, insufficient_data (<3 aftershocks), main event not found (404)
- (constraint) CONSTRAINT: Earthquake.time field addition conflicts with spec-earthquake-by-id CAP-3 constraint 'extra USGS fields (time) not included in v1'; that spec must be updated or this spec supersedes that constraint on time
- (constraint) CONSTRAINT: Clean Architecture AD-2 — Earthquake.time must be stdlib only (int or str); no datetime in domain layer (no stdlib datetime is fine, but no pydantic/external types)
- (constraint) CONSTRAINT: Repository stays thin — no get_aftershocks on the port; orchestration is pure application layer (GetAftershocksHandler calls get_by_id then get_all)
- (constraint) CONSTRAINT: sequence_assessment computed by comparing event rate in first half vs second half of window (split at midpoint of [main_shock_time, main_shock_time + days]); insufficient_data if count < 3
- (constraint) CONSTRAINT: AD-8 stateless proxy — no caching; each aftershock request hits USGS twice (once for main event, once for aftershock list)
- (constraint) CONSTRAINT: EarthquakeFilter extended in value_objects.py (the source); filters.py is a re-export shim, not the source of truth
- (constraint) CONSTRAINT: AD-4 TDD — tests written before or alongside implementation for all new handlers and routes
- (decision) sequence_assessment logic: split aftershocks into first-half and second-half by timestamp midpoint; rate = count / window_days_half; decaying if second_rate < first_rate; active if second_rate >= first_rate; insufficient_data if total < 3
- (decision) AftershockResult as stdlib dataclass in domain layer — no external imports; stats sub-dataclass or inline fields
- (decision) 404 error: when get_by_id returns None, handler raises a domain NotFound exception; interface layer catches it and returns HTTP 404
- (decision) 502 error: when USGSClient.fetch_earthquake_by_id raises httpx.HTTPStatusError (non-404), adapter propagates; interface layer catches and returns HTTP 502
- (assumption) ASSUMPTION: Earthquake.time stored as ISO-8601 string (from converting USGS ms-epoch int) rather than raw int; easier for downstream callers without adding external deps to domain
- (assumption) ASSUMPTION: get_all with lat/lon/maxradiuskm filter requires USGSAdapter to pass these through to USGSClient.query; no bounding-box alternative needed
- (question) OPEN QUESTION: Should adding the 'time' field to Earthquake entity update spec-earthquake-by-id's non-goals, or is this a deliberate supersession? Needs alignment before implementation.
- (question) OPEN QUESTION: Should USGSAdapter.get_all pass lat/lon/radius params when provided in EarthquakeFilter, or is a new method needed? Input implies extending get_all via filter fields.
- (event) Self-validate Pass 1 (Coherence): PASS — all 8 CAPs have intent+success; intents describe WHAT; 5 constraints all bend decisions; 6 non-goals explicit; success signal is demonstrable; CAP IDs 1-8 unique; prose lean
- (event) Self-validate Pass 2 (Preservation): PASS — all 17 load-bearing source claims land in SPEC.md (CAPs, Constraints, or Non-goals); wrapper-ceremony (method-stub note fragments) dropped as non-load-bearing
- (event) Conflict flagged: spec-earthquake-by-id constraint excludes Earthquake.time; this spec requires it; logged as OQ-1 and in Constraints section
- (event) Spec finalized — headless express mode; 2 open questions (OQ-1 time field conflict, OQ-2 EarthquakeFilter extension safety)
